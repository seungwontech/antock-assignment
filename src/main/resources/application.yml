spring:
  application:
    name: antock-assignment
  datasource:
    url: jdbc:h2:~/test #jdbc:h2:tcp://localhost/~/test jdbc:h2:~/test
    driver-class-name: org.h2.Driver
    username: sa
    password: ""
  jpa:
    hibernate:
      ddl-auto: create
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.H2Dialect
  h2:
    console:
      enabled: true
      path: /h2-console
juso:
  open-api:
    base-url:
    service-key:

public-data:
  open-api:
    base-url:
    service-key:
